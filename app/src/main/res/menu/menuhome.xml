<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/itmlistCrearEncuesta"
        android:title="@string/txt_list_home_view"
        app:showAsAction="never"></item>

    <item android:id="@+id/itmVerLista"
        android:title="@string/txt_list"
        app:showAsAction="never"></item>

    <item android:id="@+id/itmlisSalir"
        android:title="@string/txt_list_salir"
        app:showAsAction="never"></item>
</menu>